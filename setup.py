from setuptools import setup

setup(
   name='flaskplayground',
   version='1.0',
   description='A test application with flask',
   author='Daniel Yue',
   author_email='yuedaniel@gmail.com',
   packages=['flaskplayground'],  #same as name
   install_requires=[], #external packages as dependencies
)
